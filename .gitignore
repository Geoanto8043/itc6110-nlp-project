# DistilBERT checkpoints — too large for git (~2 GB of optimizer states)
# Re-fine-tune on Colab in ~10 min on T4 GPU
models/distilbert/

# Python
__pycache__/
*.pyc
.venv/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# Secrets
.env

# Local Claude Code settings (machine-specific paths, not shared)
.claude/settings.local.json
